{
  "name": "packsmart-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PackSmart AI Food Packaging Recommendation & Shelf-Life Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node -e \"const fs=require('fs'); fs.cpSync('frontend/dist', 'dist', {recursive: true});\"",
    "preview": "npm --prefix frontend run preview",
    "install:frontend": "npm --prefix frontend install"
  }
}
